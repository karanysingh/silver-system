
const Heading = ({ title, subTitle }) =>
    <>
        <h1 className="heading-root">
            {title}
            <br></br>
            <span>{subTitle}</span>
        </h1>
    </>

export default Heading;